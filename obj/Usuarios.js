export  class Usuario {
  constructor(nombres, apellidos, usuario, contraseña) {
    (this.nombres = nombres),
      (this.apellidos = apellidos),
      (this.usuario = usuario),
      (this.contraseña = contraseña);
  }
}
